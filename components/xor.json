{
  "id": "XOR",
  "name": "XOR Gate",
  "category": "Logic Gates",
  "description": "Outputs true when inputs are different",
  "pins": {
    "inputs": [
      { "x": -30, "y": -10 },
      { "x": -30, "y": 10 }
    ],
    "outputs": [
      { "x": 30, "y": 0 }
    ]
  },
  "logic": {
    "code": "const values = inputs.map(pin => pin.getValue());\nconst result = values.reduce((a, b) => a !== b, false);\noutputs[0].setValue(result);"
  },
  "rendering": {
    "type": "gate",
    "shape": "xor",
    "width": 60,
    "height": 40,
    "label": "=1"
  }
}
